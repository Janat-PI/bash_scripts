все скрипты создаются при помощи расширенний .sh
#! /bin/bash
#! - назвывается 'шибан' /bin/bash - bash команда интерпритатора оболочки bash
через # писать комментарий 

chmod +x script1.sh - при помощи этой команды мы можем сделать этот скрипт исполняемым 

bash команды мы можем писать и в одну строчку 

в bash программировании есть два вида переменных 1) переменная среды ($USER) 2) переменная пользователя ($name)

переменная среды вызывается через echo знак $USER и заглавными символами 

знак $ нужен чтобы вывести конкретное значения переменной

Подстановка команд-это возможность извлекать информацию из вывода команд и назначать её какой либо переменной 
Существует два способа сохранить подстановку команд 1) `pwd` 2) $(pwd)

----------------------------------------------------------------------------------------------

Условные операторы в bash script

# user_name=zhanat
# if grep $user_name /etc/passwd
# then
# echo "пользователь найден в системе!"
# cd /home/$user_name
# ls
# else
# echo "пользователь не найден в системе!"
# fi

а сравнения чисел в условных значениях идет через ключевые слова 
if [ $5 -eq $5 ]
then
echo "yeah they equal"

Ключи ( -eq <--> равно, 
        -ge <--> больше или равно,
        -gt <--> больше,
        -le <--> меньше или равно,
        -lt <--> меньше,
        -ne <--> не равно )

а в строках обычные математические операторы
> < = != 

----------------------------------------------------------------------------

циклы for в bash script 

for items in First Second Third
do
echo "$items элемент"
done

for items in "first text" "second text" "third text"
do 
echo "$items"
done 

IFS - internal fild separator
это переменная окружения в которой указывается разделитель полей

dir=/home/zhanat/*  знак * означает что мы хотим посмотреть все содержимое данной директории 
-d ключ означает директория 


